import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    static beforeLoad = {
    }

    void onInclude() {
        //
        project.name = "fish-app"
        //
        include(JavaProject).with {
            moduleDef("fish.app").with {
                depends.prod(
                    "jandcode.core.apx",
                    "jandcode.core.db.postgresql",
                )
                depends.dev(
                )
            }
        }
    }

}